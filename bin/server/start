#!/bin/bash
set -e -x
echo "shoryuken starting..."
# sudo apt-get update && apt-get upgrade -y
sudo youtube-dl -U
cd ~/vz-cpw
rvm gemset use vz-cpw
bundle
cd
sudo /etc/init.d/monit start
sudo monit start shoryuken
echo "shoryuken started."