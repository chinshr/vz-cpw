#!/bin/bash
set -e -x
echo "services starting..."

sudo youtube-dl -U
cd ~/vz-cpw
rvm gemset use vz-cpw
git pull
bundle install
foreman start &
echo $! > ~/shared/pids/launch.pid
echo "services started."
